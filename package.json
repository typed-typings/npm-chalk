{
  "name": "typed-chalk",
  "version": "1.0.0",
  "private": true,
  "description": "Typings for chalk",
  "scripts": {
    "build": "typings bundle -o test/bundle.d.ts",
    "test": "tsc -p test",
    "lint": "tslint -c tslint.json index.d.ts test/**/*.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "tslint": "^3.13.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  }
}
